name: deep_link_repository
description: A starting point for Dart libraries or applications.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: 3.10.1

dependencies:
  flutter:
    sdk: flutter
  flutter_branch_sdk: ^6.6.0

dev_dependencies:
  build_runner: ^2.1.7
  flutter_lints: ^2.0.1
